import { compatibilitySummary } from "@strk20-workbench/lab-core";

export function describeIntegration(): string {
  return `${compatibilitySummary.product} uses the ${compatibilitySummary.realRoute} for genuine execution.`;
}
