# STRK20 Workbench example

This deliberately small application will prove that another consumer can use `@strk20-workbench/lab-core` without importing the workbench frontend. The complete scenario example will be added after the core state machine is stable.
